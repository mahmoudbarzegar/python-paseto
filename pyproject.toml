[project]
name = "python-paseto"
version = "0.1.0"
description = "A pure-Python implementation of PASETO (Platform-Agnostic Security Tokens)."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "paseto>=2.0.0",
    "ruff>=0.16.2",
]
